VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=
# Optional: override callback base URL (defaults to current origin).
# Auth0 will redirect to this URL + /callback (e.g. http://localhost:3001/callback).
# Add that full URL to Auth0 Application → Allowed Callback URLs.
# VITE_AUTH0_REDIRECT_URI=http://localhost:3001
VITE_API_URL=http://localhost:4000
